import Goal from "../Goal";
import LabelingSession from "../LabelingSession";

const LabelBlock: React.FunctionComponent = () => {
    return <div>
        <Goal/>
        <LabelingSession/>
    </div>
}
export default LabelBlock;